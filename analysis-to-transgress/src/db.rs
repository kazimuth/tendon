/*

use rls_data::{Analysis, Def, Impl, MacroRef, Ref, Relation};

pub struct Storage {
    analyses: RwLock<HashMap<PathBuf, Box<Analysis>>>,
}

pub struct Db<'s> {
    storage: &'s Storage,
    dirs: Vec<PathBuf>,
}

*/
